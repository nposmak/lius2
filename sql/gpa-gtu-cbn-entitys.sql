create table gpas(
    id serial,
    gpaname varchar(150),
    primary key(id)
);

create table gtus(
    id serial,
    gtuname varchar(150),
    primary key(id)
);

create table cbns(
	id serial,
	cbnname varchar(150),
	primary key (id)
);





